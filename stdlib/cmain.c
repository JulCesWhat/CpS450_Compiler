#include <stdio.h>

int main()
{
  int x = sub(5, 3);
  writeint(0);
  writeint(x);


  int y = readint();
  writeint(y);

  y = readint();
  writeint(y);

  y = readint();
  writeint(y);

  return 0;
}
